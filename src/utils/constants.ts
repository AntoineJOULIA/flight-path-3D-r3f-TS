import { Color } from "three";

export const EARTH_RADIUS = 200;
export const CURVE_MIN_ALTITUDE = 5;
export const CURVE_MAX_ALTITUDE = 80;
export const CURVE_SEGMENTS = 32;
export const DEFAULT_TRAJECTORY_COLOR = new Color(0.1, 0.5, 0.8);
export const DEFAULT_TRAJECTORY_RADIUS = 1;
